<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cobaimage">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivityAir"></activity>
        <activity android:name=".MainActivityAgenda" />
        <activity android:name=".update_data" />
        <activity android:name=".lihat_data" />
        <activity android:name=".buat_data" />
        <activity android:name=".update_data_air" />
        <activity android:name=".lihat_data_air" />
        <activity android:name=".buat_data_air" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".Register" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>